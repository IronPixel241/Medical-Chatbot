# Empty — just to mark the folder as a package:
# It is required to make Python treat the directories as 
# containing packages; this is done to prevent directories 
# with a common name, such as 'string', from unintentionally 
# hiding valid modules that occur later on the module search path.



# Basically this empty file tells Python that 
# any files under this directory should be treated as a package.